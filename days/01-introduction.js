"use strict";
const hello = "Hello";
// * console.log() is a built-in (yerleşik) function.
console.log(hello, "World", 31 % 3, 9 ** 2);

const firstName = "yusuf",
  lastName = "Ad",
  job = "student",
  isHeAlive = true;
